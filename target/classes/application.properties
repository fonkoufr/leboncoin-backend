spring.application.name=leboncoin-api

# --- CONNEXION ORACLE CLOUD (WALLET) ---
# Vérifie bien que ce chemin C:/Projets/... est correct sur ton PC

# Attention aux slashs '/' ! Pas de '\' !
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:oracle:thin:@ok9s903rhpadr9pi_high?TNS_ADMIN=C:/Projets/Wallet_Leboncoin/Wallet_OK9S903RHPADR9PI}

# Identifiants via variables d'environnement (ne jamais écrire en dur ici)
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ADMIN}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Djoussi2017@}

# Le pilote (Driver)
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --- CONFIGURATION AUTOMATIQUE (JPA) ---
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# --- SWAGGER/SPRINGDOC CONFIGURATION ---
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha

# --- STRIPE PAYMENT ---
stripe.secret.key=${STRIPE_SECRET_KEY}